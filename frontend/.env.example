# Base URL for the FastAPI backend. In dev, leave unset — Vite proxies /api to
# http://localhost:8000 (see vite.config.ts). In production, point this at your
# deployed API, e.g. https://ads-api.yourdomain.com/api/v1
# VITE_API_BASE=/api/v1
